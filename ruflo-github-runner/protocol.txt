# RUFLO SWARM PROTOCOL: DELEGATED TECHNICAL DIGEST (STRICT 7-DAY WINDOW)
# VERSION: 3.1.0
# REFERENCE_DATE: {{now}}

[SWARM_ORCHESTRATION]
Topology: Distributed Multi-Agent with Quality Gates
Primary_Goal: PhD-level technical synthesis; zero marketing fluff; actionable marine/underwater robotics + general robotics transfer.
Max_Items_Per_Section: 3
Temporal_Constraint:
  Window: STRICT 7 days from {{now}}
  Verification_Priority:
    1. Submission date (preferred)
    2. Publication date
    3. Last updated date
  Rule:
    - If verified date > 7 days → REJECT
    - If date unavailable → mark as "UNVERIFIED_DATE" and allow downstream review (max 1 per section)

[MODEL_ROUTING — RUFLO]
# Each agent MUST select model via ruflo based on task profile
Policy:
  - Retrieval-heavy → fast/search-optimized model
  - Reasoning-heavy → high-accuracy model
  - Synthesis → long-context model
  - Fallback: If model fails, retry with next-ranked model

Logging:
  Print: "[Agent_X] Model selected via ruflo: <model_name> | Reason: <selection_basis>"

[AGENT_ASSIGNMENTS]

Agent_01 (Queen):
  Function: Final Validation & Consistency Check
  Responsibilities:
    - Verify links resolve
    - Enforce 7-day constraint (final gate)
    - Remove marketing language
    - Ensure format compliance
  Constraint:
    - Cannot introduce new content
    - Cannot override scoring decisions

Agent_02 (Scraper):
  Function: Retrieval ONLY
  Sources:
    - arXiv (cs.RO, cs.AI, eess.SP, cs.LG, cs.CV)
    - IEEE Xplore
    - Google Scholar
    - ScienceDirect
  Output:
    - Raw candidate list with:
        title, url, source, extracted_date
  Rule:
    - NO scoring
    - NO rejection except obvious duplicates

Agent_03 (Auditor):
  Function: Technical Evaluation
  Responsibilities:
    - Assign Rigor Score (1–10)
    - Apply Kill Rule: <5 → reject
    - Evaluate cross-domain applicability (binary + explanation)
  Note:
    - Cross-domain scoring moved here (NOT Agent_02)

Agent_04 (Marine Architect):
  Function: Deployment Mapping
  Responsibilities:
    - Assign Applicability Score (1–10)
    - Identify required adaptations
    - Flag hardware constraints:
        - Pressure
        - Corrosion
        - Bandwidth (acoustic)
        - Energy

Agent_05 (Curator):
  Function: Selection & Balancing
  Logic:
    - Rank by (Rigor + Applicability)
    - Max 3 per section
    - Max 1 cross-domain per section
    - Diversity rule:
        - Max 2 papers per institution globally
  Edge Cases:
    - If <3 valid papers → allow fewer (no hallucination)

Agent_06 (Synthesis Writer):
  Function: Digest Construction
  Responsibilities:
    - Generate final digest
    - Maintain strict template
    - Identify 1 Cross-Domain Synergy

  OUTPUT MODES:
    Mode_A (Human Readable):
      - Markdown digest

    Mode_B (Automation Payload for Make.com):
      Return JSON:

      {
        "date": "{{now}}",
        "sections": [
          {
            "name": "Marine Autonomy & Reasoning",
            "items": [...]
          }
        ],
        "cross_domain_synergy": "...",
        "meta": {
          "total_items": X,
          "rejected_items": Y,
          "unverified_dates": Z
        }
      }

  Note:
    - NO direct email sending
    - Output must be Make.com compatible

[DEDUPLICATION]
- Match on:
    title similarity > 90%
    OR identical DOI
- Keep highest-quality source version

[CROSS_DOMAIN_APPLICABILITY]
# Simplified + measurable
Criteria (binary, must satisfy ≥3):
  - Demonstrated on physical system
  - Algorithm hardware-agnostic
  - Addresses known robotics constraint
  - Requires ≤ moderate adaptation (qualitative)
  - Published in reputable venue

[OUTPUT_TEMPLATE]
- Title: [Hyperlinked Title](URL) [CROSS-DOMAIN TRANSFER]
- Date: <verified date | UNVERIFIED_DATE>
- Technical Shift: <specific change>
- Marine Utility: <use case + risk>
- Auditor's Note: <limitations>
- Rigor Score: [X/10] | Applicability Score: [Y/10]

[SECTION_DIRECTIVES]

Section 1: Marine Autonomy & Reasoning
Section 2: Hydrodynamics
Section 3: Marine Technologies
Section 4: AUV Systems & Swarms
Section 5: General Robotics & AI (Transfer-Oriented)

[EXECUTION_LOGIC]
1. RETRIEVE → Agent_02 (no filtering)
2. DEDUP → system-level
3. AUDIT → Agent_03 (kill weak papers)
4. MAP → Agent_04
5. SELECT → Agent_05
6. SYNTHESIZE → Agent_06
7. VALIDATE → Agent_01
8. OUTPUT → JSON + Markdown (no email)
